export function ChatPage(): JSX.Element {
  return (
    <section className="page page--chat">
      <h1>Chat</h1>
      <p className="page__lede">
        The chat experience (conversations, messages, encryption) will be implemented in
        later phases. The current foundation only wires the route and the application shell.
      </p>
    </section>
  );
}