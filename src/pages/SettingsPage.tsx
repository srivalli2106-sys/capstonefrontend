export function SettingsPage(): JSX.Element {
  return (
    <section className="page page--settings">
      <h1>Settings</h1>
      <p className="page__lede">
        Settings (profile, identity management, key export) will be implemented in later
        phases. The current foundation only wires the route and the application shell.
      </p>
    </section>
  );
}